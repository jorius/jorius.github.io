{
  "slug": "dual-gpu-local-llms",
  "category": "tech",
  "date": "2026-08-26",
  "len": "6 min",
  "tags": ["homelab", "hardware"],
  "draft": false,
  "title": {
    "en": "How to Run Local LLMs on a Dual-GPU Setup",
    "es": "Cómo correr LLMs locales con dos GPUs"
  },
  "body": {
    "en": "Two graphics cards in one desktop, running local LLMs, and none of the interesting problems were the ones I expected. This is the working configuration on `juggernaut`, plus the three specific things that cost me an evening each.\n\nIf you want the wider context of where this machine sits, that's in [the home lab post](/writing/home-lab).\n\n## The machine\n\n```\n                             ....              jorius@juggernaut\n              .',:clooo:  .:looooo:.           -----------------\n           .;looooooooc  .oooooooooo'          OS: Ubuntu 26.04.1 LTS (Resolute Raccoon) x86_64\n        .;looooool:,''.  :ooooooooooc          Kernel: Linux 7.0.0-30-generic\n       ;looool;.         'oooooooooo,          Uptime: 4 hours, 10 mins\n      ;clool'             .cooooooc.  ,,       Packages: 2683 (dpkg), 9 (flatpak), 20 (snap)\n         ...                ......  .:oo,      Shell: zsh 5.9\n  .;clol:,.                        .loooo'     Display (24G2W1G4): 1920x1080 in 24\", 144 Hz [External]\n :ooooooooo,                        'ooool     Display (24G2W1G4): 1920x1080 in 24\", 144 Hz [External]\n'ooooooooooo.                        loooo.    DE: GNOME 50.1\n'ooooooooool                         coooo.    WM: Mutter (Wayland)\n ,loooooooc.                        .loooo.    WM Theme: Yaru-dark\n   .,;;;'.                          ;ooooc     Theme: Yaru-dark [GTK2/3/4]\n       ...                         ,ooool.     Icons: Tela-circle-black-dark [GTK2/3/4]\n    .cooooc.              ..',,'.  .cooo.      Terminal: GNOME Terminal 3.58.0\n      ;ooooo:.           ;oooooooc.  :l.       CPU: AMD Ryzen 7 3700X (16) @ 4.43 GHz\n       .coooooc,..      coooooooooo.           GPU 1: NVIDIA GeForce RTX 4060 [Discrete]\n         .:ooooooolc:. .ooooooooooo'           GPU 2: NVIDIA GeForce RTX 2060 [Discrete]\n           .':loooooo;  ,oooooooooc            Memory: 19.13 GiB / 30.22 GiB (63%)\n               ..';::c'  .;loooo:'             Swap: 3.19 MiB / 8.00 GiB (0%)\n                                               Disk (/): 177.02 GiB / 912.79 GiB (19%) - ext4\n                                               Disk (/mnt/media): 1.21 TiB / 1.82 TiB (67%) - fuseblk\n                                               Local IP (enp7s0): 192.168.0.162/24\n```\n\nThe part `fastfetch` won't tell you is how the two cards are wired, and on a dual-GPU build that's the part that matters:\n\n- **RTX 4060 8 GB** &mdash; PCI `08:00.0`, CPU-attached **x8**, drives both monitors.\n- **RTX 2060 6 GB** &mdash; PCI `04:00.0`, chipset slot at **x4**, **no displays attached**.\n\nNote that `fastfetch` lists the 4060 first and the 2060 second. Hold that thought, because almost nothing else on this machine agrees with it.\n\nThe 2060 is in here because I upgraded to an ASUS TUF Gaming board with two x16 slots, and the 2060 was sitting unused in my server. I go into the very rigorous decision-making process behind that in [the home lab post](/writing/home-lab). The short version is that I wanted to see two graphics cards in one machine.\n\nWhat it left me with is a 6 GB card and an 8 GB card in the same box &mdash; 14 GB of VRAM, if you can convince anything to use both.\n\n## First: keep the compute card away from the desktop\n\nPlugging in a second NVIDIA card gave me flickering in application windows. The cause is that the compositor picks up the new card as a second KMS device and treats it as part of the seat, even with no monitor on it.\n\nOne udev rule, `/etc/udev/rules.d/74-nvidia-2060-compute-only.rules`:\n\n```\nACTION==\"add|change\", SUBSYSTEM==\"drm\", KERNEL==\"card[0-9]*\", ENV{ID_PATH}==\"pci-0000:04:00.0\", TAG-=\"seat\", TAG-=\"master-of-seat\"\n```\n\nThat drops the seat tags for that one PCI address, so the desktop stops trying to own the card and leaves it entirely for compute. Substitute your own bus ID.\n\n## The index trap\n\nThis one is nasty because everything *looks* right until you check.\n\n`nvidia-smi` numbers GPUs by PCI bus order. On this machine that means:\n\n```\n$ nvidia-smi --query-gpu=index,name,pci.bus_id --format=csv,noheader\n0, NVIDIA GeForce RTX 2060, 00000000:04:00.0\n1, NVIDIA GeForce RTX 4060, 00000000:08:00.0\n```\n\nCUDA, by default, does **not**. It sorts by capability, fastest first. The 4060 is compute 8.9, the 2060 is 7.5, so CUDA hands you `CUDA0 = 4060, CUDA1 = 2060` &mdash; exactly backwards from what you just read off `nvidia-smi`.\n\nSo `CUDA_VISIBLE_DEVICES=0`, which you set intending \"the 2060\", silently selects the 4060 instead.\n\nThe fix is one environment variable, set everywhere:\n\n```\nCUDA_DEVICE_ORDER=PCI_BUS_ID\n```\n\nThat forces CUDA to use bus order, so the indices finally agree with `nvidia-smi`. Set it before you spend any time reasoning about which card is doing what, because every conclusion you draw without it is suspect.\n\n## Ollama has no \"prefer this GPU\"\n\nHere's the one I couldn't configure my way out of. Ollama places a model on the card with the most free VRAM. Not the first card, not the ordered card &mdash; the emptiest one. I tested it: with the device order corrected and the 2060 listed first, a 4.7 GB model still landed on the 4060, because the 4060 had more room.\n\nThere is no documented environment variable that expresses a preference. Device *order* isn't placement policy.\n\nWhat does work is running two servers with different visibility, on different ports. One for light work, one for heavy work:\n\n| Server | Port | Sees | For |\n| ------ | ---- | ---- | --- |\n| light | `:11434` | the 2060 only | everyday models that fit on one card |\n| heavy | `:11435` | 2060 **+** 4060 | oversized models that need both |\n\nThey're two systemd units sharing one model directory, so nothing gets downloaded twice. The entire difference between them is three lines:\n\n```ini\n# light server — the 2060 is the only card it can see\nEnvironment=\"CUDA_VISIBLE_DEVICES=0\"\n\n# heavy server — both cards, and actually split the model across them\nEnvironment=\"CUDA_VISIBLE_DEVICES=0,1\"\nEnvironment=\"OLLAMA_SCHED_SPREAD=1\"\n```\n\nThat first one is the whole trick. Restricting the light server's *visibility* means placement policy stops mattering &mdash; there's nothing else for it to choose. And `OLLAMA_SCHED_SPREAD=1` on the heavy server is what actually splits a model's layers across both cards instead of filling one and shoving the remainder into system RAM.\n\n## The tuning that mattered\n\n- `OLLAMA_FLASH_ATTENTION=1` &mdash; cheaper attention, less VRAM per token of context.\n- `OLLAMA_KV_CACHE_TYPE=q8_0` &mdash; quantises the KV cache instead of keeping it at f16. On a 6 GB card this is the difference between a usable context window and an unusable one.\n- `OLLAMA_KEEP_ALIVE` &mdash; 15 minutes on the small server, 5 on the big one. Keeping a model resident forever is tempting, but the big server holding 14 GB hostage means the desktop has no VRAM left for anything else.\n\n## The numbers, and the surprise\n\nThe first surprise showed up when I benchmarked the two cards against each other on the same 7B, one card at a time:\n\n| Card | Generation | Load time |\n| ---- | ---------- | --------- |\n| RTX 2060 6 GB | **59.1 tok/s** | 12.2 s |\n| RTX 4060 8 GB | 53.7 tok/s | 5.8 s |\n\nThe six-year-old card generates about 10% faster than the newer one. That isn't a fluke: inference at this size is bound by memory bandwidth, not compute, and the 2060 has 30 SMs on a wider bus against the 4060's 24 on a narrower one. Newer silicon doesn't help if the bottleneck is how fast you can move weights. The 4060 wins load time by more than 2x, and *that* is the slot &mdash; x8 to the CPU versus the 2060's x4 through the chipset. Load cost is paid once per model, so with a keep-alive of minutes it stops mattering almost immediately.\n\n### What both cards together actually buy you\n\nThe harder question is what happens when a model is split across both. Every layer boundary that lands on the card seam has to push its activations over PCIe, and the 2060 is on a x4 chipset link, so the obvious fear is that the bus eats the gains.\n\nHere's the whole current lineup measured on the same prompt, same 4096 context:\n\n| Model | Server | Generation | Layers on GPU |\n| ----- | ------ | ---------- | ------------- |\n| `qwen3-abl-8b-64k` (5 GB) | 2060 dedicated | **53.4 tok/s** | 37 / 37 |\n| `qwen3-abl-14b-64k` (9 GB) | dual GPU | **28.2 tok/s** | 41 / 41 |\n| `devstral2-abl-tools:24b` (14 GB) | dual GPU | **7.5 tok/s** | 33 / 41 |\n\nThe middle row is the answer to the bus question, and it's better news than expected. The 14B genuinely lives on both cards &mdash; 4.4 GB on the 2060, 6.4 GB on the 4060, every layer on GPU &mdash; and it still turns in 28 tok/s. Roughly half the 8B's rate for roughly double the parameters, which is about what model size alone predicts. The x4 link is not the bottleneck people assume, and the reason is that a layer split only sends **activations** across the bus, not weights. That's a few megabytes per token, once per seam. The weights never move.\n\nThe bottom row is where it actually falls apart, and it isn't the bus either. The 24B is 14 GB against 14 GB of total VRAM, except the desktop is already holding about 1.4 GB of the 4060 for the two monitors. So it doesn't fit: 33 of 41 layers make it onto the GPUs, the output layer lands on the CPU, and throughput collapses to 7.5 tok/s. **Nearly a 4x drop, from 8 layers.**\n\nThat's the real lesson of a dual-GPU box. Splitting across cards is close to free. Spilling to the CPU is catastrophic. And \"the model is the same size as my total VRAM\" means it does not fit, because the desktop got there first.\n\n### How to check any of this yourself\n\nThe `/api/generate` response carries its own timings, so you don't need a stopwatch. `eval_count` divided by `eval_duration` is your tok/s:\n\n```bash\ncurl -s http://127.0.0.1:11435/api/generate -d '{\n  \"model\":\"qwen3-abl-14b-64k\",\"prompt\":\"hello\",\"stream\":false\n}' | jq '.eval_count / (.eval_duration/1e9)'\n```\n\n`/api/ps` tells you whether it actually all fit &mdash; compare `size_vram` against `size`:\n\n```bash\ncurl -s http://127.0.0.1:11435/api/ps | jq '.models[] | {name, size, size_vram}'\n```\n\nAnd the server log states the split outright, which is the number to trust:\n\n```\n$ journalctl -u ollama-big --no-pager | grep 'layers to GPU'\noffloaded 41/41 layers to GPU     # good\noffloaded 33/41 layers to GPU     # this is why it's slow\n```\n\nIf those don't match, `nvidia-smi` will show you which card ran out of room.\n\n## The models, and why they're abliterated\n\nEverything I keep loaded is an *abliterated* build &mdash; a model with its refusal behaviour ablated out.\n\n| Model | Size | Server |\n| ----- | ---- | ------ |\n| `qwen3-abl-8b-64k` | 5 GB | 2060 DEDICATED |\n| `qwen3-abl-14b-64k` | 9 GB | DUAL GPU SERVER |\n| `devstral2-abl-tools:24b` | 14 GB | DUAL GPU SERVER |\n\nThe split follows straight from the VRAM. Only the 8B fits inside the 2060's 6 GB, so it's the only one the dedicated server can hold without spilling. The 14B needs both cards and gets them cleanly. The 24B is on the dual-GPU server because that's the best available home for it, not because it fits there &mdash; as the numbers above show, it doesn't quite, and it pays for that.\n\nThis isn't about wanting a model that will help with something shady. It's that security work and reverse engineering trip refusals constantly on stock instruct models, and on every hosted API without exception. \"Explain what this shellcode does.\" \"Write a parser for this malware config block.\" \"Walk me through this obfuscated binary.\" These are the actual daily questions of the job, and they pattern-match closely enough to offensive use that the safety layer declines and offers to help with something else instead.\n\nYou can't do this work on a model that argues with you about whether the work is legitimate. A local abliterated model just answers, and the fact that it's running on hardware in my house means the questions I'm asking never leave the LAN in the first place &mdash; which for client work isn't a preference, it's a requirement.\n\n## The clients\n\n**Open WebUI** on `:11400` is the chat front-end, pointed at both servers. One gotcha worth knowing: since both servers share a model directory, each one advertises every model. Open WebUI round-robins between connections offering the same model name, which means it will happily send a small model to the both-GPU server and tie up 14 GB for nothing. Set a per-connection model allowlist &mdash; only the 8B fits the 2060, so it gets `:11434`; the 14B and 24B go to `:11435` only.\n\n**OpenCode** gets the local servers as a custom provider. The per-model context and tool-calling flags matter more than they look:\n\n```jsonc\n{\n  \"provider\": {\n    \"ollama\": {\n      \"npm\": \"@ai-sdk/openai-compatible\",\n      \"name\": \"Ollama (Juggernaut)\",\n      \"options\": { \"baseURL\": \"http://127.0.0.1:11435/v1\" },\n      \"models\": {\n        \"devstral2-abl-tools:24b\": {\n          \"name\": \"Devstral 2 24B (abliterated, tools)\",\n          \"tool_call\": true,\n          \"limit\": { \"context\": 32768, \"output\": 8192 }\n        }\n      }\n    }\n  }\n}\n```\n\nCheck `tool_call` per model before you wire anything agentic to it. Not every local model emits proper `tool_calls` &mdash; plenty of them print the JSON as plain text in the reply instead, which looks fine to a human and leaves the agent sitting there doing nothing. Test it on one trivial tool call before assuming it works.\n\n**Hermes Agent** runs against `:11435` with `qwen3-abl-8b-64k` as its default, and it's bridged into Telegram and Discord. That's the part I actually use most: the lab is reachable from my phone, through a chat app, without exposing anything to the internet &mdash; the agent reaches out, nothing reaches in. Note the `64k` in the model name; Hermes refuses any model reporting under 64K of context, which is why the models are built with an extended context window rather than the stock 32K.\n\n## Verifying it actually did what you think\n\nDon't trust the config, check the log. Ollama prints which CUDA index and which card it selected on startup:\n\n```\n$ journalctl -u ollama --no-pager | grep 'inference compute'\n... library=CUDA compute=7.5 name=CUDA0 description=\"NVIDIA GeForce RTX 2060\"\n```\n\n`compute=7.5` and the card name together confirm the pinning worked. If that says 8.9 and \"RTX 4060\", your device order is still wrong. And with a model loaded, `nvidia-smi` should show memory used on the card you expect and near-idle usage on the other.\n\nThat last check is the one I skipped for two days while being quietly confident the setup was correct.",
    "es": "Dos tarjetas gráficas en un mismo equipo, corriendo LLMs locales, y ninguno de los problemas interesantes fue el que esperaba. Esta es la configuración que funciona en `juggernaut`, más las tres cosas concretas que me costaron una noche cada una.\n\nSi quieres el contexto más amplio de dónde encaja esta máquina, está en [el post del home lab](/writing/home-lab).\n\n## La máquina\n\n```\n                             ....              jorius@juggernaut\n              .',:clooo:  .:looooo:.           -----------------\n           .;looooooooc  .oooooooooo'          OS: Ubuntu 26.04.1 LTS (Resolute Raccoon) x86_64\n        .;looooool:,''.  :ooooooooooc          Kernel: Linux 7.0.0-30-generic\n       ;looool;.         'oooooooooo,          Uptime: 4 hours, 10 mins\n      ;clool'             .cooooooc.  ,,       Packages: 2683 (dpkg), 9 (flatpak), 20 (snap)\n         ...                ......  .:oo,      Shell: zsh 5.9\n  .;clol:,.                        .loooo'     Display (24G2W1G4): 1920x1080 in 24\", 144 Hz [External]\n :ooooooooo,                        'ooool     Display (24G2W1G4): 1920x1080 in 24\", 144 Hz [External]\n'ooooooooooo.                        loooo.    DE: GNOME 50.1\n'ooooooooool                         coooo.    WM: Mutter (Wayland)\n ,loooooooc.                        .loooo.    WM Theme: Yaru-dark\n   .,;;;'.                          ;ooooc     Theme: Yaru-dark [GTK2/3/4]\n       ...                         ,ooool.     Icons: Tela-circle-black-dark [GTK2/3/4]\n    .cooooc.              ..',,'.  .cooo.      Terminal: GNOME Terminal 3.58.0\n      ;ooooo:.           ;oooooooc.  :l.       CPU: AMD Ryzen 7 3700X (16) @ 4.43 GHz\n       .coooooc,..      coooooooooo.           GPU 1: NVIDIA GeForce RTX 4060 [Discrete]\n         .:ooooooolc:. .ooooooooooo'           GPU 2: NVIDIA GeForce RTX 2060 [Discrete]\n           .':loooooo;  ,oooooooooc            Memory: 19.13 GiB / 30.22 GiB (63%)\n               ..';::c'  .;loooo:'             Swap: 3.19 MiB / 8.00 GiB (0%)\n                                               Disk (/): 177.02 GiB / 912.79 GiB (19%) - ext4\n                                               Disk (/mnt/media): 1.21 TiB / 1.82 TiB (67%) - fuseblk\n                                               Local IP (enp7s0): 192.168.0.162/24\n```\n\nLo que `fastfetch` no te dice es cómo están conectadas las dos tarjetas, y en un equipo con dos GPUs esa es justo la parte que importa:\n\n- **RTX 4060 8 GB** &mdash; PCI `08:00.0`, conectada a la CPU a **x8**, mueve ambos monitores.\n- **RTX 2060 6 GB** &mdash; PCI `04:00.0`, ranura del chipset a **x4**, **sin monitores conectados**.\n\nFíjate en que `fastfetch` lista primero la 4060 y después la 2060. Guarda ese dato, porque casi nada más en esta máquina está de acuerdo con él.\n\nLa 2060 está aquí porque actualicé a una placa ASUS TUF Gaming con dos ranuras x16, y la 2060 estaba sin usar en mi servidor. El rigurosísimo proceso de decisión detrás de eso lo cuento en [el post del home lab](/writing/home-lab). La versión corta es que quería ver dos tarjetas gráficas en una sola máquina.\n\nCon lo que me quedé fue con una tarjeta de 6 GB y una de 8 GB en la misma caja &mdash; 14 GB de VRAM, si logras convencer a algo de usar ambas.\n\n## Primero: mantener la tarjeta de cómputo lejos del escritorio\n\nConectar una segunda NVIDIA me provocó parpadeos en las ventanas de las aplicaciones. La causa es que el compositor detecta la tarjeta nueva como un segundo dispositivo KMS y la trata como parte del *seat*, incluso sin ningún monitor conectado.\n\nUna regla de udev, `/etc/udev/rules.d/74-nvidia-2060-compute-only.rules`:\n\n```\nACTION==\"add|change\", SUBSYSTEM==\"drm\", KERNEL==\"card[0-9]*\", ENV{ID_PATH}==\"pci-0000:04:00.0\", TAG-=\"seat\", TAG-=\"master-of-seat\"\n```\n\nEso quita las etiquetas de *seat* para esa dirección PCI concreta, así el escritorio deja de intentar adueñarse de la tarjeta y la libera por completo para cómputo. Sustituye tu propio bus ID.\n\n## La trampa de los índices\n\nEsta es fea porque todo *parece* correcto hasta que lo verificas.\n\n`nvidia-smi` numera las GPUs por orden de bus PCI. En esta máquina eso significa:\n\n```\n$ nvidia-smi --query-gpu=index,name,pci.bus_id --format=csv,noheader\n0, NVIDIA GeForce RTX 2060, 00000000:04:00.0\n1, NVIDIA GeForce RTX 4060, 00000000:08:00.0\n```\n\nCUDA, por defecto, **no**. Ordena por capacidad, la más rápida primero. La 4060 es compute 8.9 y la 2060 es 7.5, así que CUDA te entrega `CUDA0 = 4060, CUDA1 = 2060` &mdash; exactamente al revés de lo que acabas de leer en `nvidia-smi`.\n\nEntonces `CUDA_VISIBLE_DEVICES=0`, que configuraste pensando \"la 2060\", en silencio selecciona la 4060.\n\nEl arreglo es una sola variable de entorno, puesta en todas partes:\n\n```\nCUDA_DEVICE_ORDER=PCI_BUS_ID\n```\n\nEso obliga a CUDA a usar el orden del bus, de modo que los índices por fin coinciden con `nvidia-smi`. Configúrala antes de invertir tiempo razonando sobre qué tarjeta está haciendo qué, porque toda conclusión que saques sin ella es sospechosa.\n\n## Ollama no tiene un \"prefiere esta GPU\"\n\nEste es el que no pude resolver configurando. Ollama coloca el modelo en la tarjeta con más VRAM libre. No la primera, no la ordenada &mdash; la más vacía. Lo probé: con el orden de dispositivos ya corregido y la 2060 primero en la lista, un modelo de 4,7 GB aterrizó igual en la 4060, porque la 4060 tenía más espacio.\n\nNo existe una variable de entorno documentada que exprese una preferencia. El *orden* de dispositivos no es una política de colocación.\n\nLo que sí funciona es correr dos servidores con distinta visibilidad, en puertos distintos. Uno para trabajo ligero, otro para trabajo pesado:\n\n| Servidor | Puerto | Ve | Para |\n| -------- | ------ | -- | ---- |\n| ligero | `:11434` | solo la 2060 | modelos del día a día que caben en una tarjeta |\n| pesado | `:11435` | 2060 **+** 4060 | modelos grandes que necesitan las dos |\n\nSon dos unidades de systemd que comparten un mismo directorio de modelos, así que nada se descarga dos veces. Toda la diferencia entre ellas son tres líneas:\n\n```ini\n# servidor ligero — la 2060 es la única tarjeta que puede ver\nEnvironment=\"CUDA_VISIBLE_DEVICES=0\"\n\n# servidor pesado — ambas tarjetas, y repartir el modelo entre ellas de verdad\nEnvironment=\"CUDA_VISIBLE_DEVICES=0,1\"\nEnvironment=\"OLLAMA_SCHED_SPREAD=1\"\n```\n\nLa primera es todo el truco. Restringir la *visibilidad* del servidor ligero hace que la política de colocación deje de importar &mdash; no hay nada más que pueda elegir. Y `OLLAMA_SCHED_SPREAD=1` en el servidor pesado es lo que realmente reparte las capas del modelo entre ambas tarjetas, en vez de llenar una y empujar el resto a la RAM del sistema.\n\n## Los ajustes que sí importaron\n\n- `OLLAMA_FLASH_ATTENTION=1` &mdash; atención más barata, menos VRAM por token de contexto.\n- `OLLAMA_KV_CACHE_TYPE=q8_0` &mdash; cuantiza la caché KV en lugar de mantenerla en f16. En una tarjeta de 6 GB esta es la diferencia entre una ventana de contexto usable y una inservible.\n- `OLLAMA_KEEP_ALIVE` &mdash; 15 minutos en el servidor pequeño, 5 en el grande. Mantener un modelo residente para siempre es tentador, pero que el servidor grande retenga 14 GB significa que al escritorio no le queda VRAM para nada más.\n\n## Los números, y la sorpresa\n\nLa primera sorpresa apareció cuando medí las dos tarjetas una contra otra sobre el mismo 7B, de a una por vez:\n\n| Tarjeta | Generación | Tiempo de carga |\n| ------- | ---------- | --------------- |\n| RTX 2060 6 GB | **59,1 tok/s** | 12,2 s |\n| RTX 4060 8 GB | 53,7 tok/s | 5,8 s |\n\nLa tarjeta de hace seis años genera alrededor de un 10% más rápido que la nueva. No es casualidad: la inferencia a este tamaño está limitada por ancho de banda de memoria, no por cómputo, y la 2060 tiene 30 SMs sobre un bus más ancho frente a los 24 de la 4060 sobre uno más estrecho. El silicio más nuevo no ayuda si el cuello de botella es qué tan rápido puedes mover los pesos. La 4060 gana en carga por más del doble, y *eso* es la ranura &mdash; x8 a la CPU contra el x4 de la 2060 por el chipset. El costo de carga se paga una vez por modelo, así que con un keep-alive de minutos deja de importar casi de inmediato.\n\n### Qué te dan realmente las dos tarjetas juntas\n\nLa pregunta difícil es qué pasa cuando un modelo se reparte entre ambas. Cada frontera de capa que cae en la costura entre tarjetas tiene que empujar sus activaciones por PCIe, y la 2060 está en un enlace x4 del chipset, así que el miedo obvio es que el bus se coma la ganancia.\n\nAquí está toda la línea actual medida con el mismo prompt y el mismo contexto de 4096:\n\n| Modelo | Servidor | Generación | Capas en GPU |\n| ------ | -------- | ---------- | ------------ |\n| `qwen3-abl-8b-64k` (5 GB) | 2060 dedicada | **53,4 tok/s** | 37 / 37 |\n| `qwen3-abl-14b-64k` (9 GB) | dual GPU | **28,2 tok/s** | 41 / 41 |\n| `devstral2-abl-tools:24b` (14 GB) | dual GPU | **7,5 tok/s** | 33 / 41 |\n\nLa fila del medio es la respuesta a la pregunta del bus, y es mejor noticia de lo esperado. El 14B vive de verdad en ambas tarjetas &mdash; 4,4 GB en la 2060, 6,4 GB en la 4060, todas las capas en GPU &mdash; y aun así entrega 28 tok/s. Más o menos la mitad del ritmo del 8B por más o menos el doble de parámetros, que es justo lo que predice el tamaño del modelo por sí solo. El enlace x4 no es el cuello de botella que la gente supone, y la razón es que un reparto por capas solo manda **activaciones** por el bus, no pesos. Son unos pocos megabytes por token, una vez por costura. Los pesos nunca se mueven.\n\nLa fila de abajo es donde sí se cae todo, y tampoco es el bus. El 24B son 14 GB contra 14 GB de VRAM total, salvo que el escritorio ya está ocupando cerca de 1,4 GB de la 4060 para los dos monitores. Así que no cabe: 33 de 41 capas llegan a las GPUs, la capa de salida termina en la CPU, y el rendimiento se desploma a 7,5 tok/s. **Casi 4 veces menos, por 8 capas.**\n\nEsa es la lección real de un equipo con dos GPUs. Repartir entre tarjetas es casi gratis. Derramar a la CPU es catastrófico. Y \"el modelo pesa lo mismo que toda mi VRAM\" significa que no cabe, porque el escritorio llegó primero.\n\n### Cómo comprobar todo esto tú mismo\n\nLa respuesta de `/api/generate` trae sus propios tiempos, así que no necesitas cronómetro. `eval_count` dividido por `eval_duration` son tus tok/s:\n\n```bash\ncurl -s http://127.0.0.1:11435/api/generate -d '{\n  \"model\":\"qwen3-abl-14b-64k\",\"prompt\":\"hello\",\"stream\":false\n}' | jq '.eval_count / (.eval_duration/1e9)'\n```\n\n`/api/ps` te dice si de verdad cupo todo &mdash; compara `size_vram` contra `size`:\n\n```bash\ncurl -s http://127.0.0.1:11435/api/ps | jq '.models[] | {name, size, size_vram}'\n```\n\nY el log del servidor declara el reparto sin rodeos, que es el número en el que confiar:\n\n```\n$ journalctl -u ollama-big --no-pager | grep 'layers to GPU'\noffloaded 41/41 layers to GPU     # bien\noffloaded 33/41 layers to GPU     # por esto va lento\n```\n\nSi esos no coinciden, `nvidia-smi` te muestra a qué tarjeta se le acabó el espacio.\n\n## Los modelos, y por qué son abliterados\n\nTodo lo que mantengo cargado es una versión *abliterada* &mdash; un modelo al que se le extirpó el comportamiento de rechazo.\n\n| Modelo | Tamaño | Servidor |\n| ------ | ------ | -------- |\n| `qwen3-abl-8b-64k` | 5 GB | 2060 DEDICADA |\n| `qwen3-abl-14b-64k` | 9 GB | SERVIDOR DUAL GPU |\n| `devstral2-abl-tools:24b` | 14 GB | SERVIDOR DUAL GPU |\n\nEl reparto se desprende directamente de la VRAM. Solo el 8B cabe dentro de los 6 GB de la 2060, así que es el único que el servidor dedicado puede sostener sin derramarse. El 14B necesita ambas tarjetas y las aprovecha limpiamente. El 24B está en el servidor dual porque es el mejor sitio disponible para él, no porque quepa &mdash; como muestran los números de arriba, no termina de caber, y lo paga.\n\nEsto no se trata de querer un modelo que ayude con algo turbio. Se trata de que el trabajo de seguridad y la ingeniería inversa disparan rechazos constantemente en los modelos *instruct* de fábrica, y en todas las APIs alojadas sin excepción. \"Explica qué hace este shellcode.\" \"Escribe un parser para este bloque de configuración de malware.\" \"Recórreme este binario ofuscado.\" Estas son las preguntas reales del día a día del oficio, y se parecen lo suficiente a un uso ofensivo como para que la capa de seguridad decline y ofrezca ayudar con otra cosa.\n\nNo puedes hacer este trabajo sobre un modelo que discute contigo si el trabajo es legítimo. Un modelo abliterado local simplemente responde, y el hecho de que corra en hardware que está en mi casa significa que las preguntas que hago nunca salen de la LAN &mdash; lo cual, para trabajo con clientes, no es una preferencia sino un requisito.\n\n## Los clientes\n\n**Open WebUI** en `:11400` es el front-end de chat, apuntado a ambos servidores. Un detalle que conviene saber: como ambos servidores comparten directorio de modelos, cada uno anuncia todos los modelos. Open WebUI alterna entre las conexiones que ofrecen el mismo nombre de modelo, lo que significa que enviará alegremente un modelo pequeño al servidor de dos GPUs y ocupará 14 GB para nada. Configura una lista blanca de modelos por conexión &mdash; solo el 8B cabe en la 2060, así que se queda con `:11434`; el 14B y el 24B van únicamente a `:11435`.\n\n**OpenCode** recibe los servidores locales como proveedor personalizado. Las banderas de contexto y de tool-calling por modelo importan más de lo que parecen:\n\n```jsonc\n{\n  \"provider\": {\n    \"ollama\": {\n      \"npm\": \"@ai-sdk/openai-compatible\",\n      \"name\": \"Ollama (Juggernaut)\",\n      \"options\": { \"baseURL\": \"http://127.0.0.1:11435/v1\" },\n      \"models\": {\n        \"devstral2-abl-tools:24b\": {\n          \"name\": \"Devstral 2 24B (abliterated, tools)\",\n          \"tool_call\": true,\n          \"limit\": { \"context\": 32768, \"output\": 8192 }\n        }\n      }\n    }\n  }\n}\n```\n\nVerifica `tool_call` modelo por modelo antes de conectar cualquier cosa agéntica. No todos los modelos locales emiten `tool_calls` de verdad &mdash; muchos imprimen el JSON como texto plano en la respuesta, lo cual se ve bien para un humano y deja al agente ahí sentado sin hacer nada. Pruébalo con una llamada trivial antes de asumir que funciona.\n\n**Hermes Agent** corre contra `:11435` con `qwen3-abl-8b-64k` como modelo por defecto, y está conectado a Telegram y Discord. Esa es la parte que más uso en realidad: el laboratorio es alcanzable desde mi celular, a través de una app de chat, sin exponer nada a internet &mdash; el agente sale hacia afuera, nada entra. Fíjate en el `64k` del nombre del modelo; Hermes rechaza cualquier modelo que reporte menos de 64K de contexto, y por eso los modelos están construidos con una ventana extendida en lugar de los 32K de fábrica.\n\n## Verificar que de verdad hizo lo que crees\n\nNo confíes en la configuración, revisa el log. Ollama imprime al arrancar qué índice CUDA y qué tarjeta seleccionó:\n\n```\n$ journalctl -u ollama --no-pager | grep 'inference compute'\n... library=CUDA compute=7.5 name=CUDA0 description=\"NVIDIA GeForce RTX 2060\"\n```\n\n`compute=7.5` junto al nombre de la tarjeta confirma que el fijado funcionó. Si eso dice 8.9 y \"RTX 4060\", tu orden de dispositivos sigue mal. Y con un modelo cargado, `nvidia-smi` debería mostrar memoria en uso en la tarjeta que esperas y uso casi nulo en la otra.\n\nEsa última verificación es justo la que me salté durante dos días mientras estaba calladamente convencido de que la configuración era correcta."
  }
}
